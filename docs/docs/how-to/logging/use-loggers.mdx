---
sidebar_position: 2
title: Use loggers
---

import CodeBlock from '@theme/CodeBlock';
import MinimumLogger from "!!raw-loader!./src/minimal-logger/main.go";
import LoggerConfig from "!!raw-loader!./src/config-logger/config.dist.yml";
import ConfigLogger from "!!raw-loader!./src/config-logger/main.go";

With gosoline, there are multiple ways to configure and use a logger.

## Implement a logger directly in code

One way to implement a logger is using log functions, without appealing to an external configuration:

<CodeBlock showLineNumbers language="go" title="main.go">{MinimumLogger}</CodeBlock>

Here, you:

1. Import the `cfg` package.
2. Create a new handler for sending logs to STDOUT.
3. Create a log option that includes your handler.
4. Create your logger.
5. Apply the options.

## Implement a logger with an app configuration

If you have an application, you can create a logger from your app's configuration.

First, configure your logger in your configuration. For example, in a config file:

<CodeBlock showLineNumbers language="yml" title="config.dist.yml">{LoggerConfig}</CodeBlock>

Then, implement your logger alongside your application:

<CodeBlock showLineNumbers language="go" title="main.go">{ConfigLogger}</CodeBlock>

## Conclusion

In this guide, you've learned multiple ways to implement a logger with gosoline.

Check out these resources to learn more about logging with gosoline:

- [Implement a log handler](/how-to/logging/implement-a-log-handler)
- [API reference for the log package](/reference/package-log)
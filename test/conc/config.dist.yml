app:
  env: test
  name: ddb-lock-test
  tags:
    project: gosoline
    family: test
    group: grp
  model_id:
    pattern: "!nodecode {app.tags.project}.{app.env}.{app.tags.family}.{app.tags.group}.{modelId}"

exec:
  backoff:
    cancel_delay: 1s

cloud:
  aws:
    dynamodb:
      clients: ~

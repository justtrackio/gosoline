app:
  env: test
  name: ddb-test
  tags:
    project: gosoline
    family: test
    group: grp
  model_id:
    domain_pattern: "!nodecode {app.tags.project}.{app.env}.{app.tags.family}.{app.tags.group}"

cloud:
  aws:
    dynamodb:
      clients: ~

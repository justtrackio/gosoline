app:
  env: test
  name: ddb-test
  tags:
    project: gosoline
    family: test
    group: grp
  model_id:
    pattern: "!nodecode {app.tags.project}-{app.env}-{app.tags.family}-{app.tags.group}-{modelId}"

cloud:
  aws:
    dynamodb:
      clients: ~

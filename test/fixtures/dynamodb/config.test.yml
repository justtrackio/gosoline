app:
  env: test
  name: app
  tags:
    project: prj
    family: fam
    group: grp
  model_id:
    pattern: "!nodecode {app.tags.project}-{app.env}-{app.tags.family}-{app.tags.group}-{modelId}"


test:
  components:
    localstack:
      default:


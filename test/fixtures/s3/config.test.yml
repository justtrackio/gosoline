env: test

app_project: prj
app_family: fam
app_name: app

cloud:
  aws:
    s3:
      clients:
        default:
          usePathStyle: true

blobstore:
  test:
    bucket: s3-fixtures-test-bucket

fixtures:
  enabled: true
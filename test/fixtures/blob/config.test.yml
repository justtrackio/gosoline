env: test

app_project: prj
app_family: fam
app_group: grp
app_name: app

cloud:
  aws:
    s3:
      clients:
        default:
          usePathStyle: true

blob:
  test:
    bucket: s3-fixtures-test-bucket

fixtures:
  enabled: true
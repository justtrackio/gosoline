app:
  env: test
  name: app
  tags:
    project: prj
    family: fam
    group: grp


blob:
  default:
    bucket: default
  foo:
    bucket: foo

cloud:
  aws:
    credentials:
      access_key_id: AKID
      secret_access_key: SECRET_KEY
      session_token: TOKEN

    defaults:
      region: bla

    s3:
      clients:
        default: ~


app:
  env: test
  name: goose
  tags:
    project: gosoline
    family: integration-test
    group: grp

db:
  default:
    driver: mysql
    max_connection_lifetime: 120
    parse_time: true
    uri:
      host: 127.0.0.1
      port: 3306
      user: gosoline
      password: gosoline
      database: gosoline
    migrations:
      enabled: true
      path: migrations

fixtures:
  enabled: true

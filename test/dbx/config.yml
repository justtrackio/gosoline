app:
  env: test
  name: client_test
  tags:
    project: justtrack
    family: gosoline
    group: dbx

db:
  default:
    driver: mysql
    uri:
      host: 127.0.0.1
      port: 3306
      user: root
      password:
      database: gosoline
    migrations:
      path: .

test:
  components:
    mysql:
      default: ~

env: test

app_project: gosoline
app_family: test
app_group: grp
app_name: guard-test

db:
  default:
    driver: mysql
    max_connection_lifetime: 120
    parse_time: true
    uri:
      host: 127.0.0.1
      port: 3306
      user: root
      password: gosoline
      database: myDbName
    migrations:
      enabled: true
      table_prefixed: false
      path: file://migrations/

fixtures:
  enabled: true
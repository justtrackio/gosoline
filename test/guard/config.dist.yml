app:
  env: test
  name: guard-test
  tags:
    project: gosoline
    family: test
    group: grp

db:
  default:
    driver: mysql
    max_connection_lifetime: 120
    parse_time: true
    uri:
      host: 127.0.0.1
      port: 3306
      user: root
      password: gosoline
      database: myDbName
    migrations:
      enabled: true
      table_prefixed: false
      path: migrations

fixtures:
  enabled: true

app:
  env: test
  name: producer-daemon-test
  tags:
    project: gosoline
    family: test
    group: grp

stream:
  producer:
    testEvent:
      daemon:
        enabled: true
        aggregation_size: 3

  output:
    testEvent:
      type: sqs
      queue_id: testData

app:
  env: test
  name: kafka-producer-test
  tags:
    project: gosoline
    family: test
    group: grp

stream:
  output:
    testEvent:
      type: kafka
      topic_id: testEvent
      in_memory_override: false

test:
  components:
    kafka:
      default: ~

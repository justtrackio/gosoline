env: test

app_project: gosoline
app_family: test
app_group: grp
app_name: kafka-producer-test

stream:
  output:
    testEvent:
      type: kafka
      topic_id: testEvent
      in_memory_override: false

test:
  components:
    kafka:
      default:
        expire_after: 5m

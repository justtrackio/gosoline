env: test

app_project: gosoline
app_family: test
app_group: grp
app_name: kafka-consumer-test

stream:
  consumer:
    default:
      encoding: application/avro
  output:
    testEvent:
      type: kafka
      family: goso
      group: test
      topic_id: testEvent
      in_memory_override: false
  input:
    consumer:
      type: kafka
      family: goso
      group: test
      topic_id: testEvent
      group_id: testEvent
      in_memory_override: false
  producer:
    testEvent:
      compression: "application/snappy"
      encoding: "application/avro"

test:
  components:
    kafka:
      default:

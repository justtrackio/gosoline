app:
  env: dev
  name: stream-kinesis-output
  tags:
    project: gosoline
    family: example
    group: grp

tream-kinesis-output

stream:
  output:
    exampleRecord:
      type: kinesis
      stream_name: "{app_project}-{env}-{app_family}-{app_name}-exampleRecord"

app_project: justtrack
app_family: gosoline
app_group: examples
app_name: credentials
env: dev

cloud:
  aws:
    defaults:
      credentials:
        access_key_id: justtrack
        secret_access_key: justtrack